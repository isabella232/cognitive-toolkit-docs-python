### YamlMime:PythonClass
uid: cntk.contrib.crosstalk.RnnArgs
name: RnnArgs
fullName: cntk.contrib.crosstalk.RnnArgs
module: cntk.contrib.crosstalk
inheritances:
- cntk.contrib.crosstalk.RnnArgs
summary: "Args inside RNN variable.\n\nfw_W\n   The numpy ndarray of input projection\
  \ parameter for RNN forward in shape (input_dim, num_gates * hidden_dim)\n\nfw_H\n\
  \   The numpy ndarray of hidden projection parameter for RNN forward in shape (hidden_dim,\
  \ num_gates * hidden_dim)\n\nfw_b\n   The numpy ndarray of bias parameter for RNN\
  \ forward in shape (num_gates * hidden_dim,)\n\nbw_W\n   The numpy ndarray of input\
  \ projection parameter for RNN backward in shape (input_dim, num_gates * hidden_dim)\n\
  \nbw_H\n   The numpy ndarray of hidden projection parameter for RNN backward in\
  \ shape (hidden_dim, num_gates * hidden_dim)\n\nbw_b\n   The numpy ndarray of bias\
  \ parameter for RNN backward in shape (num_gates * hidden_dim,)"
constructor:
  syntax: RnnArgs(fw_W, fw_H, fw_b, bw_W, bw_H, bw_b)
